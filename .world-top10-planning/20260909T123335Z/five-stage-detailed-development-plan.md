# 아소비OS v3.3 5단계 상세개발계획서

- 실행 ID: `20260909T123335Z`
- 계획 상태: `PARTIAL/BLOCKED` (계획은 작성됐지만 제품 완료를 뜻하지 않음)
- 고정 순서: `STAGE-1-BASELINE` → `STAGE-2-FOUNDATION` → `STAGE-3-CORE` → `STAGE-4-QUALITY` → `STAGE-5-RELEASE`
- 각 단계의 24개 고정 절은 `stages/stage-0N/README.md`에 동일한 순서로 보관했다.
- 제품 코드·운영 데이터·공개판·Cloudflare·Sites·Git 저장 기록·비밀값은 이 계획서 작성으로 변경하지 않는다.

## 실행 전 공통 규칙

1. 단계 건너뛰기는 금지한다. 앞 단계 종료 증거가 없으면 다음 단계는 `BLOCKED`다.
2. 확인하지 않은 파일·기호·URL·수치는 `DISCOVERY-###` 작업으로 분리한다.
3. 안전 핵심 규칙을 낮은 등급으로 바꾸지 않는다. 건강·지병·문신·공포 원문은 저장하지 않는다.
4. 자료원 상태가 `unknown` 또는 `no`이면 호출·출시 목록 등록을 하지 않는다.
5. 한 미세 작업은 한 파일·한 변화만 다룬다. 즉시 가장 작은 확인을 실행한다.
6. 오류는 원문·재현 명령·복구 순서와 함께 증거에 남긴다.

## 단계 간 통과표

| 단계 | 진입 | 핵심 산출물 | 종료 조건 | 현재 |
|---|---|---|---|---|
| 1 | 프로젝트 식별 완료 | 기준선·증거·백업·범위 | 재현 가능한 기준선과 롤백 | `BASELINE_READY/BLOCKED` |
| 2 | 1단계 통과 | 데이터·권한·공통 UI 계약 | 후속 기능 계약과 공통 시험 통과 | `BLOCKED` |
| 3 | 2단계 통과 | 핵심 여정·결과 카드·복구 | 정상/빈/로딩/오류/복구 인수 통과 | `BLOCKED` |
| 4 | 3단계 통과 | 모바일·PC·접근성·성능·보안 증거 | 실제 기기·품질 조건 통과 | `BLOCKED` |
| 5 | 4단계 통과 | 출시표·복구·운영 인계 | 40개 필수 조건과 승인 확정 | `BLOCKED` |

---

# 1단계 — `STAGE-1-BASELINE` 기준선·자료 보존·안전 기반

### 목표와 사용자 결과

현재 아소비OS v3.0의 앱·PC·진행판·계정·QR, 12개 분야·88개 테마·420개 활동, E01~E11 로컬 계약, 68개 자료원 상태를 한눈에 재현한다. 사용자는 공개 주소와 “아직 사용할 수 없는 것”을 구분해 본다.

### 작업 순서

`DISCOVERY-101` 경계 확인 → `BASELINE-101` 문서/코드/시험 목록 → `URL-101` 공개 주소 목록 → `SAFETY-101` P7/P10 확인 → `EVIDENCE-101` 증거 원장 → `TEST-101` 자동 검사 → `HANDOFF-101` 인계.

### 입력·출력 계약

- 입력: `README.md`, `STATE.md`, `.project-continuity/*`, `docs/Vol1~6`, `src/shell.html`, `src/app.js`, `site/app/page.tsx`.
- 출력: 서비스 기준선 JSON/Markdown, 공개 URL 표, 미확인·차단 목록, 재현 명령, 롤백 문서.
- 빈 입력은 `없음`으로 기록하고 0점으로 바꾸지 않는다.

### 자동·수동 시험

`git status --short --branch`, JSON/CSV 구문, `python -m py_compile`, 공개 URL 읽기 전용 HEAD, `adb devices -l`을 실행한다. 기기가 없으면 실기기 시험을 하지 않는다.

### 종료·복구

기준선과 증거 ID가 모두 연결되면 종료한다. 파일 누락·권한 거부·잠금 발견 시 새 분석 폴더만 `BLOCKED`로 두고 제품 파일은 되돌리지 않는다.

### 12개 미세 작업

1. 작업 폴더 확인 — `Get-Location` — 경로 오류면 중단.
2. 연속성 파일 읽기 — `.project-continuity/STATE.md` — 누락은 `DISCOVERY-101`.
3. 잠금 읽기 — `LOCK.json` — 유효 잠금이면 읽기만 수행.
4. Git 상태 저장 — `git status --short --branch` — 실패 원인 기록.
5. 정본 문서 목록화 — `docs/Vol1~6` — 실제 경로만 사용.
6. 앱·PC 표면 목록화 — `src/shell.html`, `site/app/page.tsx`.
7. 공개 URL 후보 분리 — 기존 상태 문서의 주소만 사용.
8. 시험 명령 고정 — Python/Node/PWA 명령을 기록.
9. P7 규칙 대조 — `tools/safety_lint.py`, `packages/schema`.
10. P10 원문 저장 금지 대조 — 값은 출력하지 않음.
11. 기준선 JSON 저장 — `service-baseline.json` — 파서로 즉시 확인.
12. 인계·롤백 저장 — `stage-01/handoff.md`, `rollback.md`.

---

# 2단계 — `STAGE-2-FOUNDATION` 구조·자료·권한·공통 UI

### 목표와 사용자 결과

모든 기능이 같은 데이터 뼈대와 같은 정상·빈·로딩·오류 상태를 사용하게 한다. 사용자는 근거가 없을 때 왜 결과가 멈췄는지 알 수 있다.

### 작업 순서

`DISCOVERY-201` 기호 찾기 → `SCHEMA-201` `packages/schema` 대조 → `CATALOG-201` P1-07 출시 목록 검사 → `STATE-201` 공통 UI 상태 → `DATA-201` 입력/출력 봉투 → `TEST-201` 시험 → 인계.

### 입력·출력 계약

입력은 활동 ID·조건·자료 근거·신선도·안전 상태를 포함한다. 출력은 `go/caution/no/unknown`, 이유 배열, 사용 자료원, 다음 행동을 포함한다. 건강·문신·공포 원문과 승인되지 않은 자료원은 제외한다.

### 자동·수동 시험

NaN·무한대·숫자형 bool·잘못된 목록/URL·만료 자료·빈 출시 목록·P7 강등 시도를 거부한다. 키보드 초점과 모바일/PC 상태 문구를 비교한다.

### 종료·복구

계약·상태·차단 검사가 통과하면 종료한다. 골든 시험이 깨지거나 안전 등급이 내려가면 변경을 저장하지 않고 원래 기준선으로 돌아간다.

### 12개 미세 작업

1. 스키마의 등급·안전 기호 찾기.
2. E02 입력 봉투와 필수 필드 대조.
3. P1-07 출시 목록 형식 읽기.
4. 빈 목록의 의도된 실패 확인.
5. 공통 정상 상태 문구 작성.
6. 공통 빈 상태 문구 작성.
7. 공통 로딩 상태와 초점 작성.
8. 만료·권한 거부 오류 작성.
9. 오프라인·재시도·초기화 행동 작성.
10. P7/P10 음성 대조 시험 연결.
11. JSON/CSV·정적 검사 실행.
12. 계약·인계·롤백 증거 저장.

---

# 3단계 — `STAGE-3-CORE` 핵심 사용자 여정·필수 기능

### 목표와 사용자 결과

사용자가 “활동 선택 → 조건 입력 → 결과 → 이유 → 다음 행동 → 저장/공유/초기화”를 앱과 PC에서 끝까지 이용한다.

### 작업 순서

`DISCOVERY-301` 화면 기호 확인 → `JOURNEY-301` 여정 상태 → `RESULT-301` 결과 카드 → `RECOVERY-301` 오류 복구 → `AUTH-301` 외부 계정 로그인(OAuth) 경계 → 12개 핵심 시험 → 인계.

### 입력·출력 계약

결과 카드에는 판정, 이유, 자료 시각, 신선도, 제한 문구, 다음 행동을 표시한다. 위세이버 로그인 시작 주소는 연결하지만 Asobi callback·세션 저장이 없는 동안에는 로그인 완료로 표현하지 않는다.

### 자동·수동 시험

정상·빈·로딩·부분 성공·오류·오프라인·잘못된 입력·만료 자료·중복 클릭·초기화·공유·권한 거부를 각각 시험한다. 실제 로그인 정보는 입력하지 않는다.

### 종료·복구

모든 핵심 상태와 이유가 화면·시험·증거로 연결되고 E02·자료원 승인도 끝나야 `CORE_DONE`이다. 안전 근거 없는 `go`가 나오면 공개 플래그를 끄고 결과를 보존한다.

### 12개 미세 작업

1. 활동 선택 상태 찾기.
2. 조건 입력 필드 고정.
3. 입력 오류 분류.
4. 결과 카드 필드 고정.
5. 정상 계산 흐름 연결 위치 확인.
6. 빈/로딩 상태 연결.
7. 오류 복구 버튼 연결.
8. 로컬 저장 경계 확인.
9. 공유 데이터에서 토큰 제거.
10. OAuth 외부 시작만 확인.
11. 12개 핵심 시험 실행.
12. 인계·롤백 문서 저장.

---

# 4단계 — `STAGE-4-QUALITY` 품질·접근성·보안·운영

### 목표와 사용자 결과

휴대폰·PC·느린 망·오프라인·키보드·스크린리더에서도 사용자가 결과와 다음 행동을 잃지 않는다.

### 작업 순서

`DEVICE-401` ADB 확인 → `A11Y-401` 접근성 → `PERF-401` 성능/느린 망 → `SEC-401` 개인정보·보안 → `OPS-401` 관측 → 전체 재검사.

### 입력·출력 계약

오프라인 결과에는 마지막 계산 시각·자료 신선도를 표시한다. 접근성 정보는 색·아이콘·문장을 함께 사용한다. 운영 기록에 민감 원문을 넣지 않는다.

### 자동·수동 시험

`node --test site/tests/rendered-html.test.mjs`, PWA 검사, ESLint, P7/P10 검사, 자산 크기, 키보드, 스크린리더, 실제 기기 설치·오프라인·재연결·삭제를 수행한다. ADB에 기기가 없으면 해당 시험은 미실시다.

### 종료·복구

기기 미검출·스크린리더 미실시·성능 예산 초과는 `QUALITY_BLOCKED`다. 시험 전 백업을 확인하고, 문제가 생기면 분석 산출물만 되돌린다.

### 12개 미세 작업

1. `adb devices -l` 읽기.
2. 유휴 기기 한 대 선택.
3. HTTPS 설치 경로 확인.
4. 키보드 순서 검사.
5. 스크린리더 이름 검사.
6. 느린 망 재현.
7. 오프라인 전환.
8. 재연결 복구.
9. 저장소·로그 민감값 검색.
10. 자산 크기·첫 화면 측정.
11. 자동 품질 묶음 실행.
12. 기기·결과·증거·롤백 저장.

---

# 5단계 — `STAGE-5-RELEASE` 공개·복구·운영 인수

### 목표와 사용자 결과

공개 주소·앱·PC·QR·계정·개인정보 안내가 서로 맞고, 문제가 생기면 이전 상태로 안전하게 돌아간다.

### 작업 순서

`RELEASE-501` 증거 모음 → `LEGAL-501` 사람·법무·안전 승인 확인 → `CUTOVER-501` 공개 대상 소유권 확인 → `REGRESSION-501` 전체 재검사 → `ROLLBACK-501` 복구 연습 → 운영 인수.

### 입력·출력 계약

공개판은 버전·자료 시각·제한 문구·문의 경로를 표시한다. 로그인·광고·결제는 실제 활성화 전까지 비활성으로 표시한다.

### 자동·수동 시험

40개 필수 조건, 링크·QR·개인정보 안내·광고 안내, 전체 Python/PWA/렌더링 검사, 공개 URL 읽기 확인, 유휴 Android 1대 최종 시험, 실제 복구 연습을 각각 기록한다.

### 종료·복구

필수 조건 하나라도 미확인·실패이면 공개하지 않는다. 공개 대상이 404·관리 불가·비용 미확인이면 기존 주소를 보존하고 전환하지 않는다.

### 12개 미세 작업

1. 40개 필수 조건 수집.
2. E02 사람 서명 확인.
3. 승인 자료원 수 확인.
4. OAuth 시작·callback·세션 계약 확인.
5. Sites/Cloudflare 소유권 읽기.
6. 앱·PC·진행판·QR 주소 확인.
7. 전체 재검사 실행.
8. 이전 버전 복구 연습.
9. 개인정보·광고·문의 문구 확인.
10. 공개 후 관측 항목 확인.
11. 인수 패키지 검증.
12. `run-manifest.json`에 최종 판정 저장.

---

## 작업카드·시험·인계 공통 계약

- [96필드 작업카드](development-task-cards.json)의 모든 카드는 96개 판정 필드와 최소 12개 미세 작업 자리를 가진다.
- 일반 작업은 최소 12개, 안전·권한·삭제·공개·이사 작업은 최소 24개의 고유 시험을 추가한다.
- 연결은 `EVIDENCE → GAP → DECISION → REQUIREMENT → STAGE → TASK → STEP → TEST → ACCEPTANCE` 순서를 유지한다.
- 현재 `READY` 카드는 0개다. 실제 파일·기호·명령·외부 증거가 확인된 뒤에만 `READY`로 올린다.
- [5단계 위험·롤백 등록부](../../.product-intelligence-v33/20260909T123335Z/five-stage-risk-and-rollback-register.md)와 각 단계 `handoff.md`를 함께 읽는다.

## 현재 최종 판정

이 계획서는 저숙련 AI가 다음 작업을 시작할 수 있을 정도로 단계·순서·조건·시험·복구·인계를 명시했다. 그러나 E02 사람 안전 검토, 68개 자료원 승인, 로그인 서버, 실제 기기, 세계 Top10 고정군, Readiness 엔진 실행이 남아 있으므로 제품 100%나 공개 승인으로 보고하지 않는다.
